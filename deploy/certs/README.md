# TLS certificates

Place the staging certificate chain in `fullchain.pem` and its private key in `privkey.pem`.
Certificate files are ignored by Git and must be provisioned directly on the staging host.
